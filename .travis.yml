language: python
python:
  - "2.7"
node_js:
  - "0.10"
rvm:
  - "2.0.0"
before_install:
  - "npm install -g bower"
  - "npm install -g grunt-cli"
  - "gem install bundler"
install:
  - npm install
script:
  - npm test
